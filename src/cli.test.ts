import { describe, it } from 'vitest';

describe('parseCommand', () => {
  it.todo('parses an import command with its CSV filename');
  it.todo('parses a lookup command with its partner member ID');
  it.todo('returns undefined for missing or unsupported arguments');
});

describe('usage', () => {
  it.todo('returns instructions for importing and looking up members');
});
