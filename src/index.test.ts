import { describe, it } from 'vitest';

describe('main CLI flow', () => {
  it.todo('prints usage and sets a failure exit code for an invalid command');
  it.todo('imports members for the import command');
  it.todo('prints the matching member or a not-found message for lookup');
});
